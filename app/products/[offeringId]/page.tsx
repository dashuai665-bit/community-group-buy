import{ProductDetail}from'@/components/storefront/product-detail';export default async function ProductPage({params,searchParams}:{params:Promise<{offeringId:string}>;searchParams:Promise<{community?:string}>}){const[{offeringId},{community}]=await Promise.all([params,searchParams]);return <section className="content-section page-top">{community?<ProductDetail offeringId={offeringId} communityId={community}/>:<p role="alert">缺少社區資訊，請從社區商品頁重新進入。</p>}</section>}
